# Loumar Auth Relay

Auth relay service for Loumar ecosystem apps. Handles authentication forwarding between internal services.

## Stack
- Node.js
- PM2 (process manager)

## Files
- `server.js` - Main server entry point
- `ecosystem.config.js` - PM2 configuration
- `package.json` - Dependencies
- `HISTORY.md` - Change history
